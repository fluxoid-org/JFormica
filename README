
File -> import -> General -> existing projects into workspace -> choose this folder

I've included a linux x86_64 javax usb implementation. You may need to recompile/ use an alternative implementation.

Build in eclipse
